import React from "react";
import { Icon } from 'antd';

const Icons = ({ type }) => {
    return (
       
                <Icon type={type} theme="filled" />
           
    )
}

export default Icons;